- tab: "Scatter plot of global data"
  testcases:
    - expression:
        python: "submission.scatter_plot_global_data([]) == None"
      return: true
    - expression:
        python: "submission.fetch_csv.__doc__ is not None"
      return: true

